import 'package:flutter/material.dart';

class ChatModuleBox extends StatelessWidget {
  const ChatModuleBox({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
